name: My First Workflow

on: push

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello GitHub Actions!"
        
      - name: Show Date
        run: date

      - name: Show Current Folder
        run: pwd